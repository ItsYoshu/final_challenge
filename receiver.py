def receive(bits):
    # This function should return two values as a dictionary
    # {"state":True, "message":"Hello"}
    # state : Return TRUE if the input is the same message that was sent (GUESS). Return False if not.
    # message : Return the translated message.
    return
